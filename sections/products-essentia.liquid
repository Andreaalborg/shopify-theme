<section class="products-essentia section">
  <div class="container">
    
    <!-- Section Header -->
    <div class="section-header text-center fade-in">
      <div class="section-badge">
        <span class="badge-text">Våre Produkter</span>
      </div>
      <h2 class="section-title">Premium Luftfriskere</h2>
      <p class="section-description">
        Tre unike produkter designet for moderne livsstil. Hver med sin egen personlighet og anvendelse.
      </p>
    </div>
    
    <!-- Products Grid -->
    <div class="products-grid">
      
      <!-- Product 1: Car Vent -->
      <article class="product-card card-large fade-in">
        <div class="product-visual">
          <div class="product-image-placeholder">
            <div class="product-icon-large icon-vent">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                <path d="M3 7v10a2 2 0 002 2h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2z"/>
                <path d="M8 9l8 0"/>
                <path d="M8 12l8 0"/> 
                <path d="M8 15l8 0"/>
              </svg>
            </div>
          </div>
        </div>
        <div class="product-content">
          <div class="product-category">Bil Tilbehør</div>
          <h3 class="product-name">Car Vent Luftfrisker</h3>
          <p class="product-description">
            Diskré og kraftfull luftfrisker som festes enkelt i bilens ventilasjon. Aktiveres av luftstrømmen og gir jevn duft i opptil 45 dager.
          </p>
          
          <div class="product-features">
            <div class="feature-item">
              <span class="feature-icon">⏱️</span>
              <span>45 dager varighet</span>
            </div>
            <div class="feature-item">
              <span class="feature-icon">🎛️</span>
              <span>Justerbar intensitet</span>
            </div>
            <div class="feature-item">
              <span class="feature-icon">🔧</span>
              <span>Universal passform</span>
            </div>
          </div>
          
          <div class="product-footer">
            <div class="product-price">149 kr</div>
            <div class="product-actions">
              <a href="/products/car-vent" class="btn btn-primary">Kjøp nå</a>
              <a href="/products/car-vent" class="btn btn-ghost">Mer info</a>
            </div>
          </div>
        </div>
      </article>
      
      <!-- Product 2: Mirror Hanger -->
      <article class="product-card card-compact fade-in">
        <div class="product-visual-compact">
          <div class="product-image-placeholder">
            <div class="product-icon-large icon-mirror">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                <circle cx="12" cy="12" r="7"/>
                <path d="M12 5v14"/>
                <path d="M5 12h14"/>
              </svg>
            </div>
          </div>
        </div>
        <div class="product-content-compact">
          <div class="product-category">Premium Design</div>
          <h3 class="product-name">Mirror Hanger</h3>
          <p class="product-description">
            Elegant luftfrisker som henger diskré i bakspeilet. Kombinerer stilfullt design med langvarig duft.
          </p>
          <div class="product-footer">
            <div class="product-price">99 kr</div>
            <a href="/products/mirror-hanger" class="btn btn-primary">Kjøp nå</a>
          </div>
        </div>
      </article>
      
      <!-- Product 3: Home Diffuser -->
      <article class="product-card card-compact fade-in">
        <div class="product-visual-compact">
          <div class="product-image-placeholder">
            <div class="product-icon-large icon-diffuser">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                <path d="M12 2v20"/>
                <path d="M17 5H9.5a3.5 3.5 0 000 7h5a3.5 3.5 0 010 7H6"/>
              </svg>
            </div>
          </div>
        </div>
        <div class="product-content-compact">
          <div class="product-category">Hjemme Wellness</div>
          <h3 class="product-name">Home Diffuser</h3>
          <p class="product-description">
            Avansert ultralydteknologi som skaper den perfekte atmosfæren med duft, lys og ro.
          </p>
          <div class="product-footer">
            <div class="product-price">399 kr</div>
            <a href="/products/home-diffuser" class="btn btn-primary">Kjøp nå</a>
          </div>
        </div>
      </article>
      
    </div>
    
    <!-- Call to Action -->
    <div class="section-cta text-center fade-in">
      <h3>Ikke sikker på hvilket produkt som passer best?</h3>
      <p class="text-muted mb-lg">La oss hjelpe deg med å finne den perfekte luftfriskeren for dine behov.</p>
      <a href="/pages/kontakt" class="btn btn-secondary">Kontakt oss</a>
    </div>
    
  </div>
</section>

<style>
  .products-essentia {
    background: var(--secondary);
  }
  
  .section-header {
    max-width: 600px;
    margin: 0 auto var(--spacing-2xl);
  }
  
  .section-badge {
    display: inline-flex;
    align-items: center;
    padding: 6px 16px;
    background: var(--white);
    border: 1px solid var(--border);
    border-radius: 100px;
    margin-bottom: var(--spacing-md);
  }
  
  .badge-text {
    font-size: 0.85rem;
    font-weight: 500;
    color: var(--primary);
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }
  
  .section-title {
    font-size: clamp(2rem, 4vw, 2.5rem);
    font-weight: 700;
    margin-bottom: var(--spacing-md);
  }
  
  .section-description {
    font-size: 1.125rem;
    color: var(--text-muted);
    margin-bottom: 0;
  }
  
  /* Products Grid */
  .products-grid {
    display: grid;
    grid-template-columns: 2fr 1fr 1fr;
    grid-template-rows: 1fr 1fr;
    gap: var(--spacing-md);
    margin-bottom: var(--spacing-2xl);
  }
  
  .product-card {
    background: var(--white);
    border-radius: var(--radius-md);
    border: 1px solid var(--border);
    overflow: hidden;
    transition: all 0.3s ease;
    display: flex;
    flex-direction: column;
  }
  
  .product-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 16px 48px rgba(0,0,0,0.12);
    border-color: var(--accent);
  }
  
  /* Large Card */
  .card-large {
    grid-row: 1 / 3;
    grid-column: 1 / 2;
  }
  
  .product-visual {
    height: 300px;
    background: linear-gradient(135deg, var(--secondary), var(--accent));
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    overflow: hidden;
  }
  
  .product-visual::before {
    content: '';
    position: absolute;
    top: -50%;
    right: -50%;
    width: 100%;
    height: 100%;
    background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, transparent 70%);
    animation: rotate 20s linear infinite;
  }
  
  @keyframes rotate {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }
  
  .product-content {
    padding: var(--spacing-lg);
    flex: 1;
    display: flex;
    flex-direction: column;
  }
  
  /* Compact Cards */
  .card-compact:nth-child(2) {
    grid-row: 1 / 2;
    grid-column: 2 / 3;
  }
  
  .card-compact:nth-child(3) {
    grid-row: 2 / 3;
    grid-column: 2 / 3;
  }
  
  .product-visual-compact {
    height: 160px;
    background: linear-gradient(135deg, var(--secondary), var(--accent));
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
  }
  
  .product-content-compact {
    padding: var(--spacing-md);
    flex: 1;
    display: flex;
    flex-direction: column;
  }
  
  /* Common Styles */
  .product-image-placeholder {
    width: 100px;
    height: 100px;
    background: rgba(255,255,255,0.9);
    border-radius: var(--radius-md);
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    z-index: 2;
  }
  
  .product-icon-large {
    width: 48px;
    height: 48px;
    color: var(--primary);
  }
  
  .product-category {
    font-size: 0.8rem;
    font-weight: 500;
    color: var(--text-muted);
    text-transform: uppercase;
    letter-spacing: 0.5px;
    margin-bottom: var(--spacing-xs);
  }
  
  .product-name {
    font-size: 1.25rem;
    font-weight: 600;
    margin-bottom: var(--spacing-sm);
  }
  
  .product-description {
    color: var(--text-muted);
    margin-bottom: var(--spacing-md);
    flex: 1;
  }
  
  .product-features {
    margin-bottom: var(--spacing-md);
    display: flex;
    flex-direction: column;
    gap: var(--spacing-xs);
  }
  
  .feature-item {
    display: flex;
    align-items: center;
    gap: var(--spacing-xs);
    font-size: 0.9rem;
    color: var(--text-muted);
  }
  
  .feature-icon {
    font-size: 1rem;
  }
  
  .product-footer {
    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: var(--spacing-sm);
    margin-top: auto;
  }
  
  .product-price {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--primary);
  }
  
  .product-actions {
    display: flex;
    gap: var(--spacing-xs);
  }
  
  .product-actions .btn {
    padding: 10px 16px;
    font-size: 0.9rem;
  }
  
  /* Section CTA */
  .section-cta {
    max-width: 500px;
    margin: 0 auto;
    padding: var(--spacing-lg);
    background: var(--white);
    border-radius: var(--radius-md);
    border: 1px solid var(--border);
  }
  
  .section-cta h3 {
    font-size: 1.25rem;
    margin-bottom: var(--spacing-sm);
  }
  
  /* Responsive */
  @media (max-width: 1024px) {
    .products-grid {
      grid-template-columns: 1fr 1fr;
      grid-template-rows: auto auto auto;
    }
    
    .card-large {
      grid-row: 1 / 2;
      grid-column: 1 / 3;
    }
    
    .card-compact:nth-child(2) {
      grid-row: 2 / 3;
      grid-column: 1 / 2;
    }
    
    .card-compact:nth-child(3) {
      grid-row: 2 / 3;
      grid-column: 2 / 3;
    }
    
    .product-visual {
      height: 200px;
    }
  }
  
  @media (max-width: 768px) {
    .products-grid {
      grid-template-columns: 1fr;
      grid-template-rows: auto;
    }
    
    .card-large,
    .card-compact:nth-child(2),
    .card-compact:nth-child(3) {
      grid-row: auto;
      grid-column: 1;
    }
    
    .product-visual,
    .product-visual-compact {
      height: 180px;
    }
    
    .product-footer {
      flex-direction: column;
      align-items: stretch;
      text-align: center;
    }
    
    .product-actions {
      justify-content: center;
    }
    
    .section-cta {
      padding: var(--spacing-md);
    }
  }
</style>

{% schema %}
{
  "name": "Products Essentia",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Section title",
      "default": "Premium Luftfriskere"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Section description",
      "default": "Tre unike produkter designet for moderne livsstil."
    }
  ],
  "presets": [
    {
      "name": "Products Essentia"
    }
  ]
}
{% endschema %}