<div class="page-template section">
  <div class="container">
    <h1 class="page-title animate-on-scroll">{{ page.title }}</h1>
    <div class="page-content animate-on-scroll">
      {{ page.content }}
    </div>
  </div>
</div>

<style>
  .page-template {
    min-height: 60vh;
    padding-top: 120px;
  }
  
  .page-title {
    text-align: center;
    margin-bottom: 60px;
  }
  
  .page-content {
    max-width: 800px;
    margin: 0 auto;
    line-height: 1.8;
  }
  
  .page-content h2 {
    margin-top: 40px;
    margin-bottom: 20px;
  }
  
  .page-content p {
    margin-bottom: 20px;
  }
</style>